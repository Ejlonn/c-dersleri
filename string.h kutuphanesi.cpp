#include <stdio.h>
#include <string.h>


int main(){
	
	char string1[] = "sa";
	char string2[] = "bro";
	
	//strlwr(string1);   / string1 i küçük harflere dönüþtürücek
	//strupr(string1); / string1i  büyük harflere dönüþtürcek
	//strcat(string1,string2);    string2yi string 1in sonuna eklicek
	//strncat(string1,string2,1);  string 2 deki sayý kadar harfi string 1in sonuna eklicek  yani sab olcak
	//strcopy(string1,string2);  string2 yi string 1 e kopyalýcak yani string1 = bro
	//strncopy(string1,string2,1);    string2deki 1 karakteri string 1e kopyalýcak  yani string1 = ba
 	//strset(string1, ?); string 1 deki tum karakterleri ? 'ne çevircek.
 	//strnset(string1,?,1);   string1deki   baþtan 1 karakteri ?'ne çevircek
 	//strrev(string1); string1 i ters çevircek
 	
   //int result = strlen(string1);                          // returns string length as int
   //int result = strcmp(string1, string2);         // string compare all characters
   //int result = strncmp(string1, string2, 1);   // string compare n characters
   //int result = strcmpi(string1, string1);        // string compare all (ignore case)
   //int result = strnicmp(string1, string1, 1);  // string compare n characters (ignore case)
 	
 	
	
	
}
