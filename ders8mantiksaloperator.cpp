#include <stdio.h>
/*

Bool veri tipi true or false

Mantýksal ifade doðru ise = true
Mantýksal ifade yanlýþ ise = false
ayný zamanda 0 olmayan tüm sayýlar (1,132,50000...) true deðerdir
0 ise false deðerdir

Mantýksal operatörler

< ------> Küçük müdür. a<b þeklinde
> ------> Büyük müdür. a>b þeklinde
<= -----> Küçük ya da Eþit midir. a<=b þeklinde
>= -----> Büyük ya da Eþit midir. a>=b þeklinde
== -----> eþit midir. a==b þeklinde
!= -----> Eþit deðil midir a!=b þeklinde
b < c ? b: c;     bu b küçük c ise b, deðilse c demek.
&& -----> Mantýksal and (ve) baðlacý. true && true && false gibi baðlanabilir. && olan cümlelerin true olmasý için bütün ifadelerin true olmasý gerekir.
Eðer bu ifade içerisinde bir tane bile false varsa mantýksal cümle false olur.
|| -----> Mantýksal or (ya da) baðlacý. true || true || false gibi baðlanabilir. || olan cümlelerin true olmasý için en az bir ifadenin true olmasý gerekir.
Eðer bu ifade içerisinde tüm ifadeler false ise mantýksal cümle false olur.
! ------> mantýksal not (deðili) operatörü. doðru olan (true) mantýksal ifadeyi yanlýþ (false)yapar, yanlýþ olan (false) ifadeyi true yapar.

Þartlý cümleler
a= x>y ? x: y;

*/

